{
  "name": "spacework-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "echo 'E2E removed — run Playwright locally if needed'"
  },
  "dependencies": {
    "@trystero-p2p/torrent": "^0.24.0",
    "three": "^0.165.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.6",
    "jsdom": "^29.1.1",
    "vite": "^5.2.0",
    "vitest": "^4.1.6"
  }
}
